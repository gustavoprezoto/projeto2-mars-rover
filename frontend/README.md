# Frontend - Projeto 2 ES47B

Aplicação React simples com Login, Busca e Inserção.

## Como rodar

```bash
cd frontend
npm install
npm run dev
```

Por padrão, o frontend espera o backend em `http://localhost:3000`.
Você pode alterar isso via variável `VITE_API_BASE` no `.env` do Vite.
